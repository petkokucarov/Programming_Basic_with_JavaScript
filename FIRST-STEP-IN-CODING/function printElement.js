function printElement(input) {
    let elements = input;

        for (let index = 0; index < elements.length; index++) {
            console.log(elements[index]);
            
        }
        
    }

    printElement(["1", "2", "3"]);